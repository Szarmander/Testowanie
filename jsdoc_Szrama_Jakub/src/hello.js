/**
 * shows alert `Hello <somebody>`
 * @param {string} somebody name of user
 * @returns {undefined} None
 * @author Jakub Szrama 5D
 */
const sayHello = (somebody) => {
  alert(`Hello ${somebody}`);
};
